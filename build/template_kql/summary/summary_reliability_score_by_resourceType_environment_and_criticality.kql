${extend_resource}
| project Type, SkuName, Kind, SubscriptionId, Name, State, Location, ResourceGroup, Zones, AvailabilityZone, AvailabilitySet, Capacity, FaultDomain, OSDisk, ReadReplica, AutomaticFailover, MultipleWriteLocations, StorageAutogrow, ReplicationRole, Backup = tolower(Backup), Environment, ResourceId
| ${summarize_score}
    by Type, Environment
| ${calculate_score}
    | project Type, Environment, ResourceTotal, tostring(ReliabilityScore)
    | sort by Type