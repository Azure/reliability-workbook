${extend_resource}
|project Type, SkuName, ['High Availability'] = HighAvailability, GeoRedundantBackup, Zone, SubscriptionId, Name, State, Location, ResourceGroup
| where Type == 'microsoft.dbforpostgresql/flexibleservers'