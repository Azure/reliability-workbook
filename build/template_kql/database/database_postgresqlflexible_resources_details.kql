${extend_resource}
| project Type, SkuName, State, Location, ResourceGroup, HighAvailability, GeoRedundantBackup, Zone, SubscriptionId, Name
| where Type == 'microsoft.dbforpostgresql/flexibleservers'