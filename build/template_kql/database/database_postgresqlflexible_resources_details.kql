${extend_resource}
| project Type, SkuName, State, Location, ResourceGroup, Zones, azone, HighAvailability, GeoRedundantBackup, SubscriptionId,, ReplicationRole, Name, Environment
| where Type == 'microsoft.dbforpostgresql/flexibleservers'