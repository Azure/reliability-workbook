${extend_resource}
| project Type, SkuName, State, Location, ResourceGroup, Zones, azone, HighAvailability, GeoRedundantBackup, SubscriptionId, Name, Environment
| where Type == 'microsoft.dbforpostgresql/flexibleservers'