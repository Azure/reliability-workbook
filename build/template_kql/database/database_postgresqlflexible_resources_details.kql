${extend_resource}
| project Type, SkuName, State, Location, ResourceGroup, Zones, azone, HighAvailability, GeoRedundantBackup, SubscriptionId, Name, ReplicationRole, Environment
| where Type == 'microsoft.dbforpostgresql/flexibleservers'